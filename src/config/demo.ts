/**
 * The fully-featured showcase gym that prospective owners explore via the
 * "See an example gym" button. Because it is a demo, its public pages fall back
 * to the built-in sample content (workouts, coaches) so every feature is visible
 * even though no real data is entered - the sample data is generated relative to
 * today, so it never goes stale.
 */
export const DEMO_GYM_SLUG = 'my-new-gym';
